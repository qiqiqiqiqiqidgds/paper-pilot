# PaperPilot Backend - Python Dependencies
# 适用于 Python 3.11+（app/api/analyze.py 使用了 asyncio.timeout，为 3.11+ API）

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1
# 0.0.9 存在 CVE-2024-24762（ReDoS）/ CVE-2024-53981（boundary DoS），锁定已修复版本
python-multipart==0.0.32

# 数据校验
pydantic==2.7.1
pydantic-settings==2.2.1

# PDF 解析
# 1.24.7 过旧（<1.26），升级到较新稳定版（要求 Python >=3.10，与 requirements.txt 声明一致）
pymupdf==1.28.0

# DOCX 解析
python-docx==1.1.2

# LLM（OpenAI 兼容 SDK；当前接 MiniMax，亦兼容 DeepSeek 等）
# P3-2: 移除 tiktoken（全项目无任何 import，属死依赖）
openai==1.35.7

# HTTP 客户端（联网搜索备用）
httpx==0.27.0

# XML 安全解析（P2-5）：arXiv Atom feed 按 Python 官方 XML 漏洞矩阵建议
# 使用 defusedxml 解析，防实体展开 / 外部实体注入（search_client._parse_feed）
defusedxml==0.7.1

# PPT 生成（W6）
# 锁定 1.0.2（1.x 系列的稳定版；>=1.0.2 会跟着 minor/patch 自动走，加了 <2.0 防止 2.x 破坏性变更）
python-pptx==1.0.2

# 配置 & 工具
python-dotenv==1.0.1

# 结构化日志（Sprint 2 / I1+I2）：JSON 输出 + request_id 关联
python-json-logger==2.0.7

# 异步
anyio==4.4.0
