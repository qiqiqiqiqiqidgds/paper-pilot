# PaperPilot 前端环境变量
# 复制为 .env.local

# 后端 API 地址（服务端 BFF 代理使用，默认 http://localhost:8000）
# 前端通过 /api/proxy 转发，浏览器 bundle 不会包含后端地址
BACKEND_URL=http://localhost:8000

# 与后端 APP_API_KEY 保持一致（仅服务端使用，不会暴露到浏览器）
# 后端未配置 APP_API_KEY 时留空即可
BACKEND_API_KEY=
